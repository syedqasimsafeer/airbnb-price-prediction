# Artifacts Folder

This folder is reserved for optional project artifacts such as metadata, plots, or saved outputs.

For Streamlit Cloud deployment, the app generates everything automatically at runtime.
